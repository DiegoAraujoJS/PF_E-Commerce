let initialState = {
    professors: ['asd','aaa'],
    professor: []
};

const all = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
};

export default all;
