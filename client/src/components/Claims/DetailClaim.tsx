import React from "react";

function DetailClaim(props){

    //let { code:code } = useParams();
    //let[code] = useState(code);
    
    //let { name, code: codeClaim, detail } = claim;


    return(
        <div>
            {/* <h2>name: {name}</h2>
            <p>descripcion: {descriptio}</p>
            <span>#{code}</span>
            <p>{detail}</p> */}
            <h2>nombre</h2>
            <p>descripcion</p>
        </div>
    )
}

export default DetailClaim;